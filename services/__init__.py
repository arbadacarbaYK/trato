"""Runtime services for Trato (relay subscription, escrow).

Kept separate from the pure protocol core (`trato.mostro`, `trato.nostr`) so the
core stays unit-testable without a network or LNbits.
"""
