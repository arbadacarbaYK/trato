"""Pure (LNbits-independent) Mostro protocol layer for the Trato extension.

These submodules deliberately avoid importing anything from LNbits so they can
be unit-tested in isolation and reused by both the client and operator roles.
"""
